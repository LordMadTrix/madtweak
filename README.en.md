<div align="center" markdown="1">
  <img src="assets/logo.png" alt="MadTweak Logo" width="300" />

  # 🔴 MadTweak v1.4

  **Windows 10 / 11 optimisation you stay in control of**

  *A PowerShell utility for optimisation, privacy, appearance and maintenance — with real undo.*

  [![Windows](https://img.shields.io/badge/OS-Windows_10_%7C_11-0078D6?style=for-the-badge&logo=windows&logoColor=white)](https://www.microsoft.com/windows)
  [![PowerShell](https://img.shields.io/badge/PowerShell-5.1-5391FE?style=for-the-badge&logo=powershell&logoColor=white)](https://learn.microsoft.com/powershell/)
  [![Version](https://img.shields.io/badge/Version-1.4-ff003c?style=for-the-badge)](https://github.com/LordMadTrix/madtweak/releases/latest)
  [![Licence](https://img.shields.io/badge/Licence-MIT-2ea44f?style=for-the-badge)](LICENSE)

  **English** · [Français](README.md)
</div>

<div align="center">
  <img src="assets/interface.png" alt="The MadTweak interface" width="100%" />
  <p><sub><i>The interface: profiles, 150 tweaks in tabs with their explanations, and a live log.</i></sub></p>
</div>

---

> ### 🌍 Fully bilingual
>
> MadTweak follows your Windows display language automatically — interface, menus, and
> all 150 tweak titles and explanations. Force it with `-Langue en` or `-Langue fr`, or
> pick a language from the header of the window.
>
> The project is written in French first: French speakers are poorly served by this kind
> of tool, and that stays its identity. English was added to make it usable elsewhere,
> not to replace it.

---

## ⚡ What it does

### 🛡️ **Privacy & cleanup**
- ✅ **150 reversible tweaks**: telemetry, ads, Copilot, Recall, Store bloatware
- ✅ **5 ready-made profiles** (Minimal, Clean UI, Privacy, Gamer, Laptop)
- ✅ **Measured cleanup**: every reclaimable item is weighed *before* anything is purged

### 🔍 **Understand your machine**
- ✅ **Health score out of 100**, broken down by category
- ✅ **Startup analysis**: real boot time and the cost in seconds of each startup program
- ✅ **Crash diagnosis**: cross-references recent unexpected shutdowns with active power tweaks
- ✅ **Drift detection**: spots settings a Windows update silently reverted

### ↩️ **Real undo**
- ✅ **Backed up before written**: every value touched is recorded first
- ✅ **Exact restore**, all at once or tweak by tweak
- ✅ **Simulation mode**: see everything, write nothing

### 💾 **Install Windows without sitting through it**
- ✅ **Downloads the official ISO** from Microsoft, no third-party tool
- ✅ **Builds the USB stick**: FAT32 format, image splitting, native UEFI boot
- ✅ **Account, language, time zone, edition, applications** decided up front
- ✅ **A MadTweak profile applied at first boot**, clean machine from the start
- ✅ **Verified end to end** on real hardware, not just on paper

### 🎨 **Interface & personalisation**
- ✅ **Themeable GUI** (6 themes) or 16 console menus
- ✅ Windows accent colour, wallpapers **generated by code**
- ✅ **ASUS ROG extras**: RGB keyboard driven over raw HID, GPU and fan sensors

---

## 🚀 Install

Download **[`MadTweak.ps1`](https://github.com/LordMadTrix/madtweak/releases/latest)**
(a single, self-contained file), then in an **elevated** terminal:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\MadTweak.ps1
```

> Windows may show a SmartScreen warning: the script isn't code-signed. All of the
> source is in this repository, and **Simulation mode** lets you see exactly what would
> change before anything is written.

**Requirements** — Windows 10 (22H2) or Windows 11 (22H2 → 25H2), administrator rights,
Windows PowerShell 5.1 (ships with Windows).

> **Why PowerShell 5.1 and not 7?** Deliberate. The Appx cmdlets used to remove
> bloatware are native in 5.1; under PowerShell 7 they go through a slow compatibility
> layer. Running it in 7 would *degrade* it. The script detects this and warns you.

---

## 🛡️ What makes it safe

An optimiser that breaks a machine hasn't optimised anything. These rules live in the
code, not in a promise.

- **A tweak refuses to do harm.** Rather than applying a setting that would hurt *this
  particular* machine, it raises an explanatory error: SysMain on a mechanical hard
  drive, the Ultimate Performance power plan on a laptop, forced S3 sleep on a modern
  standby platform — a real case that was causing crashes on resume from hibernation.
- **Nothing reports success if it failed.** A tweak that can't act prints `[FAILED]`
  with its reason. There are no silent successes.
- **Measured, never assumed.** System drive type, NPU presence, Windows edition, cache
  sizes, network adapters — all read at runtime.
- **No placebo.** Popular but inert or harmful tweaks are deliberately excluded, with
  the reason documented in a comment: `LargeSystemCache`, standby list flushing,
  disabling IPv6.
- **Every change goes through a gate.** Registry → `Set-RegValue` / `Remove-RegValue` /
  `Remove-RegKey`. Executables → `Invoke-Externe`. Everything else → `Invoke-Action`.
  A change bypassing these gates would do real damage during simulation.
- **Backed up before written.** `Save-EtatAvant` (values), `Save-EtatService`
  (services), `Save-EtatCle` (whole keys, exported as `.reg`). The first state seen is
  the true one — an entry is never overwritten.

### The six safety nets

They catch the failures that silence would hide: a mistyped key produces no error at
all, just a tweak that never fires.

| Net | When | Catches |
|---|---|---|
| `Test-ClesProfils` | startup | a profile key matching no tweak |
| `Test-CoherenceAudit` | startup | an audit testing a key no tweak sets |
| `Test-Explications` | startup | a selectable tweak **with no explanation** |
| `$script:ClesJouees` tally | end of a run | a valid key that is **unreachable** |
| `Test-AutounattendXml` | every answer file generated | a setting placed in a pass where Windows would **silently ignore** it, a wiped disk with no install target, a command over 1024 characters |
| CI (`build.ps1 -Verifier`) | every push | `dist/` no longer matching `src/` |

---

## 💾 Install media: the answer file

Menu `15` (and the **Install media** tab) generates an `autounattend.xml` — the answer
file Windows Setup reads on startup. Dropped at the root of your USB stick, next to
`setup.exe`, it decides language, time zone, edition, the account to create, the
applications to install, and the MadTweak profile to apply at first logon. You get a
clean, configured machine without sitting in front of the screen.

**No Windows image is shipped** — the Microsoft licence forbids redistributing it. You
download the official ISO yourself; this file simply sits next to it. Same rule as the
wallpapers: **generate, don't bundle**. What comes out is plain text you can read first.

Language, keyboard and time zone are not guessed: the **"Same as this PC"** option reads
the current machine's actual configuration, and the time zone list is the one Windows
really knows (141 on an ordinary machine), current zone first. A keyboard layout copied
wrong is only discovered once you are sitting in front of the installed machine, typing
your password on the wrong layout.

**The edition isn't guessed either.** An edition name absent from the image makes setup
fail. Rather than have you bet on it, MadTweak **opens your ISO** (or `install.wim` /
`install.esd`), reads the editions it actually contains and offers those. The image is
mounted read-only and dismounted whatever happens.

Three things to know before using it:

- **A password in an answer file is not encrypted.** It is base64-encoded, which anyone
  holding the USB stick can read back in one command. Leaving the field empty creates an
  account with no password, to be set at first boot — safer.
- **Nothing is erased unless you ask.** By default Setup asks the usual question and you
  pick your partition. Automatic wiping of disk 0 exists, but requires typing `EFFACER`
  in full (console) or confirming an alert dialog (GUI).
- **An already-customised image may ignore this file.** A sysprepped "home-made" build
  carries its own answer file in `C:\Windows\Panther`, with its passes marked as processed:
  the one on your stick then never takes over. The `-ForcerPassages` parameter exists for
  that case and **only** that case — on a stock image it breaks the install. Observed both ways.

### Measured, not assumed

The whole path was exercised on real hardware, with a **stock Microsoft ISO downloaded by
MadTweak** (Windows 11 25H2, French):

| Step | Result |
|---|---|
| FAT32 media built by MadTweak | 4.3 min, **native UEFI boot**, no third-party loader |
| Microsoft account screen | skipped |
| Local account | created on its own |
| Profile applied at first boot | 27 tweaks out of 33 |

The 6 failures are the ones expected on a fresh machine, and the tool names them: winget not
provisioned yet, no Fax service, Widgets policy refused.

One caveat often repeated elsewhere did **not** hold here: Microsoft's new setup engine
(`SetupPrep.exe`, "ConX", introduced in 24H2) is reputed to ignore the `oobeSystem` pass. On
this 25H2 image it honoured it fully. One run is not a rule — but it beats a warning copied
from somewhere else.

---

## 🐧 On Linux? See **MadOS ROG Edition**

MadTweak has a sibling for the other half of the dual boot:
**[MadOS ROG Edition](https://github.com/LordMadTrix/MadOS_ROG_Edition)** turns a fresh
Kubuntu 24.04 into an ASUS ROG gaming station (XanMod kernel, GPU drivers, `asusctl`,
themed KDE).

| | [MadOS ROG Edition](https://github.com/LordMadTrix/MadOS_ROG_Edition) | MadTweak |
|---|---|---|
| **System** | Kubuntu 24.04 LTS | Windows 10 / 11 |
| **Role** | **Installs and transforms** a fresh system | **Tunes and cleans** an existing one |

*Same philosophy, same ROG identity — two systems, two tools.*

---

## 🛠️ Building from source

The shipped file is **built**, not hand-edited.

```
Lancer.bat             double-click: UAC elevation + PowerShell 5.1
build.ps1              the tool: src/ → dist/
src/                   26 source modules — order comes from the name (00-, 10-, 20-…)
dist/MadTweak.ps1      the PRODUCT — generated, do not edit
```

```powershell
.\build.ps1              # builds dist\MadTweak.ps1
.\build.ps1 -Verifier    # checks it's up to date, writes nothing (exit 1 otherwise)
```

> **Encoding is not a detail.** Every `.ps1` must be **UTF-8 WITH BOM** and use **LF**
> line endings. Without the BOM, Windows PowerShell 5.1 reads the file as ANSI and
> `GÉNÉRÉ` becomes `GÃ‰NÃ‰RÃ‰`. `Lancer.bat` follows the **opposite** convention:
> ASCII, CRLF, no BOM. `.gitattributes` and `build.ps1` enforce both.

See [CONTRIBUTING.md](CONTRIBUTING.md) before opening a pull request.

---

## 🗺️ Roadmap

- [x] French interface, 150 tweaks, audit, exact undo
- [x] Health score, startup / disk / crash analysis
- [x] Continuous integration
- [x] **English interface** — language mechanism, then all 150 tweaks translated
- [ ] Code signing certificate (removes the SmartScreen warning)

---

## 💛 Thanks

MadTweak is built in my spare time and will stay free. Thanks to everyone supporting it
through [GitHub Sponsors](https://github.com/sponsors/LordMadTrix) — sponsors who want
to be are credited here.

Thanks as well to anyone who **reports a bug**, **suggests a tweak** or **tests on a
setup I don't own**: that's what keeps a tool that touches the registry safe on machines
I can't test myself.

## 📜 Licence

**MIT License** — free and open source. Made with ❤️ by LordMadTrix.

**MadTweak is free, and will stay that way.** No "Pro" version, no gated features, no
ads, no data collection.
